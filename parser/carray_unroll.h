#ifndef CARRAY_UNROLL_H
#define CARRAY_UNROLL_H

// unrolls declaration of array initializers - this way no special code is needed in the code generator
list_t *c_array_unroll(list_t *lp);

#endif // CARRAY_UNROLL_H
